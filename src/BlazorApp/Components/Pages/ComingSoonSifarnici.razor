@page "/sifarnici"
@rendermode InteractiveServer
@inject NavigationManager Nav

<PageTitle>Šifarnici — Digitalizacija procjene nekretnina</PageTitle>

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo("/admin/sifarnici", replace: true);
    }
}
